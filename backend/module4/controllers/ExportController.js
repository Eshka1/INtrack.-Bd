const ExportService=require('../services/exportService');
class ExportController{static async download(req,res){try{const format=String(req.query.format||'xlsx').toLowerCase();if(format==='xlsx'){const buffer=await ExportService.buildXlsx(req.companyId);res.setHeader('Content-Type','application/vnd.openxmlformats-officedocument.spreadsheetml.sheet');res.setHeader('Content-Disposition',`attachment; filename="INTrack_${req.companyId}_export.xlsx"`);return res.send(Buffer.from(buffer));}if(format==='pdf'){const buffer=await ExportService.buildPdf(req.companyId);res.setHeader('Content-Type','application/pdf');res.setHeader('Content-Disposition',`attachment; filename="INTrack_${req.companyId}_summary.pdf"`);return res.send(buffer);}return res.status(400).json({message:'format must be xlsx or pdf.'});}catch(error){return res.status(500).json({message:error.message});}}}
module.exports=ExportController;
