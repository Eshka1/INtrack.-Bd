jest.mock('../../../models/Notification',()=>({find:jest.fn(),findOneAndUpdate:jest.fn()}));const N=require('../../../models/Notification');const C=require('../../../controllers/NotificationController');const {createMockResponse}=require('../../helpers/mockResponse');
describe('NotificationController',()=>{beforeEach(()=>jest.clearAllMocks());test('list',async()=>{N.find.mockReturnValue({sort:jest.fn(()=>({limit:jest.fn(()=>({lean:jest.fn().mockResolvedValue([{_id:'N1'}])}))}))});const res=createMockResponse();await C.list({companyId:'C1'},res);expect(N.find).toHaveBeenCalledWith({companyId:'C1'});expect(res.json).toHaveBeenCalledWith([{_id:'N1'}]);});test('markRead 404',async()=>{N.findOneAndUpdate.mockResolvedValue(null);const res=createMockResponse();await C.markRead({companyId:'C1',params:{id:'X'}},res);expect(res.status).toHaveBeenCalledWith(404);});test('markRead success',async()=>{N.findOneAndUpdate.mockResolvedValue({_id:'N1',read:true});const res=createMockResponse();await C.markRead({companyId:'C1',params:{id:'N1'}},res);expect(res.json).toHaveBeenCalledWith({_id:'N1',read:true});});test('list error',async()=>{N.find.mockImplementation(()=>{throw new Error('db');});const res=createMockResponse();await C.list({companyId:'C1'},res);expect(res.status).toHaveBeenCalledWith(500);});});
