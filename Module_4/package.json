{
  "name": "intrack-module4-final-simple",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "setup": "npm install --prefix frontend && npm install --prefix backend",
    "dev": "concurrently \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\""
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}