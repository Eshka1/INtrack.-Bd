import axios from 'axios';const api=axios.create({baseURL:'/api/module4'});api.interceptors.request.use(c=>{const t=localStorage.getItem('accessToken');if(t)c.headers.Authorization=`Bearer ${t}`;return c;});
export async function getAuditLogs(params={}){return (await api.get('/audit-logs',{params})).data;}export async function getSuperAdminCompanies(){return (await api.get('/super-admin/companies')).data;}export async function getSuperAdminAnalytics(){return (await api.get('/super-admin/analytics')).data;}export async function overrideSubscription(companyId,subscription_id){return (await api.patch(`/super-admin/companies/${companyId}/subscription`,{subscription_id})).data;}export async function getNotifications(){return (await api.get('/notifications')).data;}export async function markNotificationRead(id){return (await api.patch(`/notifications/${id}/read`)).data;}
export async function downloadTenantExport(format){const r=await api.get('/export',{params:{format},responseType:'blob'}),url=URL.createObjectURL(new Blob([r.data],{type:r.headers['content-type']})),a=document.createElement('a');a.href=url;a.download=format==='pdf'?'INTrack_summary.pdf':'INTrack_export.xlsx';document.body.appendChild(a);a.click();a.remove();URL.revokeObjectURL(url);}
